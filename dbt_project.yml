name: 'elvis'
version: '1.0.0'
config-version: 2

profile: 'elvis'

model-paths: ["models"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
clean-targets: ["target", "dbt_packages"]

models:
  elvis:
    staging:
      +materialized: view
    marts:
      +materialized: table
