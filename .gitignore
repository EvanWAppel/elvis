*.csv
# ...but the CI seed fixtures must be tracked (they stand in for raw.* in CI).
!seeds/
!seeds/*.csv
.venv
.dbt
.env
.user.yml
__pycache__/
*.pyc
target/
logs/

# Generated warehouse — rebuild with `uv run python build_warehouse.py && uv run dbt build`
*.duckdb
*.duckdb.wal
